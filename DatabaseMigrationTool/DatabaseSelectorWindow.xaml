<Window x:Class="DatabaseMigrationTool.DatabaseSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Databases" 
        Height="500" Width="400" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F5F5">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Database List -->
        <Border Grid.Row="0" BorderBrush="#D0D0D0" BorderThickness="1" 
                Background="White" Margin="0,0,0,15">
            <ListBox Name="lstDatabases" 
                    SelectionMode="Multiple"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding}" FontSize="12" Margin="2" Height="22"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Bottom Controls -->
        <Button Grid.Row="1" Name="btnClose" Content="Close" 
               Click="Cancel_Click" 
               Padding="20,8"
               Background="#9E9E9E" Foreground="White" 
               BorderThickness="0" 
               HorizontalAlignment="Right"/>
    </Grid>
</Window>
